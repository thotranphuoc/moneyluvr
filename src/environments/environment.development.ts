/**
 * Development env. Use generate-env.js or set SUPABASE_URL / SUPABASE_ANON_KEY locally (do not commit).
 */
export const environment = {
  production: false,
  supabase: {
    url: '',
    anonKey: ''
  }
};
